[pytest]
# pytest-django wires the test runner to Django; tests use the PostGIS test DB
# Django creates from the configured database (see WebGIS/settings.py).
DJANGO_SETTINGS_MODULE = WebGIS.settings
python_files = tests.py test_*.py *_tests.py
